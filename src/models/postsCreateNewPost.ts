export type PostsCreateNewPost = {
    /**
     * Posts create new post with title, shortDescription, content and blogId:
     */
    title: string,
    shortDescription: string,
    content: string,
    blogId: string
}